import { DataSource } from 'typeorm';

/**
 * Sample data for development/testing
 * Run with: npm run seed (if configured)
 */
export async function seedDatabase(_dataSource: DataSource): Promise<void> {
  // This is a placeholder for seeding logic
  // Can be extended with actual seeding functionality
  console.log('Database seeding is not configured yet.');
  console.log('To enable, create a seed script or CLI command.');
}
